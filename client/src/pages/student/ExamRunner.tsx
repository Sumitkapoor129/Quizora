import { EmptyState } from '@/components/ui/EmptyState';

/** Temporary Phase 3 stub — replaced by the real exam screen in Phase 5. */
export default function ExamRunner() {
  return (
    <>
      <h1 className="page-heading">Exam</h1>
      <EmptyState title="Coming soon" description="The exam screen arrives in a later phase." />
    </>
  );
}