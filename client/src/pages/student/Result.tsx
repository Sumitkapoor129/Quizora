import { EmptyState } from '@/components/ui/EmptyState';

/** Temporary Phase 3 stub — replaced by the real result view in Phase 7. */
export default function Result() {
  return (
    <>
      <h1 className="page-heading">Result</h1>
      <EmptyState title="Coming soon" description="Your result arrives in a later phase." />
    </>
  );
}